// var fs = require('fs');
var turf = require('bindings')('turf_file')
turf.init_(null);

// var validShapes = [[0,1,2,3], [0,2,3,4]];
// // var badShapes = ['[0,1,2,3]', null]

// var validTurfs = [{name: 'ok'}, {name:'no', size:100}];
// // var badTurfs = [{name: 'ok'}];

// var out;
// console.log(turf.pack(validShapes, validTurfs));
// // // console.log(out[0], out[1], out[2], out[3], out[4]);
// // fs.writeFileSync('output', out);
// // // console.log(turf.pack(['a'], ['b']));
// // // console.log(turf.pacl(validShapes, validTurfs));
